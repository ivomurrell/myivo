{
  "name": "autobivo",
  "version": "1.0.0",
  "description": "Personal site for Ivo Murrell",
  "main": "index.js",
  "devDependencies": {
    "@types/jquery": "^3.5.14",
    "@types/three": "^0.83.3",
    "esbuild": "^0.14.38",
    "typescript": "^4.6.3"
  },
  "scripts": {
    "check": "tsc -noEmit",
    "build": "npm run check && esbuild ts/main.ts --bundle --sourcemap --outfile=build/app.js",
    "build:production": "npm run check && esbuild ts/main.ts --bundle --minify --outfile=build/app.min.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ivo Murrell",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ivoisbelongtous/myivo.git"
  }
}
